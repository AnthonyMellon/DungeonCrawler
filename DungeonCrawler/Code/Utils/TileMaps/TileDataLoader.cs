using DungeonCrawler.Code.Data;
using System.Collections.Generic;

namespace DungeonCrawler.Code.Utils.TileMaps
{
    internal static class TileDataLoader
    {
        /*public static List<List<Tile>> LoadTilesFromRoomData(RoomData roomData)
        {
            TileData[] tileData = DefaultContent.GetTileData;
            List<List<Tile>> tiles = new List<List<Tile>>();

            for (int x = 0; x < roomData.Tiles.Count; x++)
            {
                List<int> dataRow = roomData.Tiles[x];
                List<Tile> tileRow = new List<Tile>();

                for (int y = 0; y < dataRow.Count; y++)
                {
                    for (int z = 0; z < tileData.Length; z++)
                    {
                        if (tileData[z].ID == dataRow[y]) tileRow.Add(new Tile(

                            ));
                    }
                }
            }
        }*/
    }
}
